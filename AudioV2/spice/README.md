# AudioV2 回路シミュレーション

設計判断の根拠として回した ngspice ネットリスト。**判断そのものは
[AGENT_HANDOFF.md](../AGENT_HANDOFF.md) §2.9 と [DECISIONS.md](../DECISIONS.md) §11.1 が正。**
ここはその根拠となった波形を再現するためのもの。

```bash
ngspice -b AudioV2/spice/<file>.cir
```

## `ampbank_switch_pop.cir` — 出力カップリングの置き場所

`AmpBank` でアンプ出力をバスへ繋ぐとき、カップリングコンデンサをアナログスイッチの
**前**に置くか**後**に置くか。後に置けば全 ch 共通の1組で済むが、前に置くと ch 毎に要る。

オペアンプの DC オフセット（NE5532 代表 ±3 mV）を持つ 2 台の間で切り替え、
バスに出る段差を測る。信号を載せると段差が振幅に埋もれるので DC のみで観測する。

| | バスに出る段差 |
|---|---|
| 案A: カップリングが SW の前 | **≈ 0 V**（4e-18） |
| 案B: カップリングが SW の後（共通1組） | **−6.0 mV**、150 ms かけて減衰 |

6 mV は信号 7 V に対し −61 dB でアンプの雑音フロアの約3000倍。低周波過渡なので
「ボツッ」と聴こえる。**案A（ch 毎に前置き）を採用。**

> **⚠ このデッキの限界（2026-09-02 追記）:** 案A側の `a1s`（`Ca1` とスイッチの間）には
> 漏れ電流源を入れていない。そのため「前置きにすると今度はカップリング C の右極板が
> 非選択中に浮く」という論点をこのデッキは検証していない。実際その節点は
> `ampbank_input_pulldown.cir` が扱ったのと同じ構造で、**出力側にも 220 kΩ の
> プルダウンが要る**（2026-09-02 に追加）。2.2 µF に 0.15 nA で 68 µV/s、
> 非選択 1 分で 4 mV。ここで案B を却下した根拠の −6 mV を数分で超える。
出力カップリングを 470 µF → 2.2 µF に小さくしたことで、ch 毎に持っても
面積が 3.6 cm² に収まる（470 µF なら 24.5 cm² で不可能だった）。

## `ampbank_input_pulldown.cir` — 入力 220 kΩ プルダウンの置き場所

現行 AmpModule の入力プルダウン（`R701`/`R706` = 220 kΩ）を、スイッチの
**前**（音源側）と**後**（結合コンデンサの左極板側）のどちらに置くか。

スイッチのオフ漏れ電流（TMUX7612 の max 0.15 nA）を注入し、非選択を 5 秒続けた
あとに再接続する。

| | 非選択中の結合C左極板 | 再接続時の段差 |
|---|---|---|
| 案P: 220 kΩ が SW の**後** | **0.000033 V** | なし |
| 案Q: 220 kΩ が SW の**前** | **150 V**（浮いて漏れ電流で充電） | **−149 V** |

案Q の 150 V は理想モデルゆえの誇張（実機では ESD ダイオードや基板漏れでクランプ
される）だが、「浮いた節点が漏れ電流で充電され再接続時に放電する」機序は実在し、
数百 mV〜数 V の段差になる。**案P（SW の後ろへ移す）を採用。**

これにより 220 kΩ の役割は「ケーブル未接続時に入力を浮かせない」から
**「非選択中に結合コンデンサの左極板を 0 V に保つ」**へ変わる。10 ch すべてに必要。

## `switch_thd.py` — アナログスイッチが足す THD（2026-09-02）

「TMUX7612 は高いので DG412 で足りないか」という検討のために作った。
**`ΔRon / R_load` は THD ではない**（あれは非線形分圧の大きさの目安で、
実際の THD は `Ron(V)` 曲線の形で決まる。TI SCDA058 も同じ説明）ので、
データシートのグラフを **PDF のベクタパスから数値として抽出**し、
時系列を解いて FFT した。目視の読み取りではない。

    data/dg412_ron_curve.json     Vishay 61564 の ±15V カーブ（68点）
    data/tmux7612_ron_curve.json  TI Figure 5-4 の ±15V カーブ（151点）

### 分かったこと1: TMUX7612 の平坦領域は ±11V で終わる

| 信号範囲 | TMUX7612 | DG412 |
|---|---:|---:|
| ±6.5 V（入力側） | 0.001 Ω※ | 1.59 Ω |
| ±13 V（出力側） | **0.313 Ω** | 5.70 Ω |
| ±15 V | 6.309 Ω | 5.70 Ω |

※ グラフの分解能限界。仕様表は 0.0003 Ω。**グラフから mΩ 精度を主張しないこと。**

TI が DS 本文で「best linear performance には上下レールから 5V の headroom」と
書いているのが数値で裏付けられた（±15V 電源なら ±10V まで）。

### 分かったこと2: 出力振幅に膝がある

通し（入力SW → OpAmp×2 → 出力SW → 50kΩ）の THD、Rbias 47k:

| 出力 | TMUX7612 | DG412 |
|---|---:|---:|
| 7.07 Vrms（±10V） | **−162.8 dB** | −90.9 dB |
| 9.2 Vrms（±13V、現仕様） | −115.2 dB | −90.0 dB |
| 10.0 Vrms（±14.1V） | −88.3 dB | −91.2 dB |

**現仕様の 9.2 Vrms は TMUX7612 の性能を 47 dB 捨てている。** ±14V では DG412 に逆転される。

### 分かったこと3: DG412 は出力側で律速される

DG412 は Rbias をいくら上げても通しで −90 dB 止まり（出力側スイッチが −93 dB で律速、
しかも振幅を下げても改善しない）。**出力を理想スイッチ（機械リレー）にすると**
入力側の真の限界が見え、Rbias 220k で −113 dB、1M で −126 dB まで伸びる。

### 手法の妥当性

- **linear と PCHIP の差は 0.1 dB 以内。** cubic spline は点間でオーバーシュートして
  偽の曲率＝偽の THD を作るので使わない
- `ΔRon/R_load` の目安は実際の THD より 11〜14 dB 悲観的だった

## `switch_offiso.py` — OFF の9chから何が漏れるか（2026-09-02）

ON のスイッチの THD だけ見ていては足りない。10ch セレクタでは**非選択9chの OFF 経路**が
別の律速になりうる。`CS(off)` の比では判断できない（あれはソース対グランド容量）。
**ソース→ドレインの貫通は OISO 規格が捉えている**ので、そこから実効直列容量を逆算する。

| | OISO @100kHz (RL=50Ω) | 逆算 C_off |
|---|---:|---:|
| TMUX7612 | −105 dB | **0.18 pF** |
| TMUX4821 | −50 dB | **100.6 pF**（562倍） |

10ch を節点方程式で解いた結果（Rbias 47k / 負荷 50k / 1 ON・9 OFF）:

| 周波数 | TMUX7612 BUS漏れ | TMUX4821 BUS漏れ | TMUX4821 の特性誤差 |
|---:|---:|---:|---:|
| 1 kHz | −126 dB | −71 dB | 0.0024 dB |
| 20 kHz | −100 dB | **−46.8 dB** | **0.040 dB** |

**漏れは信号の同相コピーなので THD にはならない**（周波数特性の誤差になる）。
非選択アンプ自身の歪みが漏れてくる分は −145 dB で無視できる。
つまり OISO の差は TMUX4821 を失格にしない。

ただし **20kHz では非選択アンプの入力に −5.9 dB も漏れる**（TMUX7612 は −59.5 dB）。
高域では10台全部が信号を増幅している。害はないが「非選択chは休んでいる」は成り立たない。

## `adg1407_ron_curve.json` — ADG1407 の実測（2026-09-02）

「入力を10 DUTへ常時ブロードキャストし、出力だけ切替える」構成の検討で、ADG1407（ADI、
dual 8:1 差動マルチプレクサ、EN付き）が2 IC で10ch分の出力選択を賄える候補として浮上した。
データシート（`AudioV2/datasheets/ADI_ADG1406_1407.pdf`）の **Figure 10**（VDD=+15V/VSS=−15V
固定、TA=−40/+25/+85/+125℃の4本）をベクタパスから数値抽出した（TMUX7612/DG412と同じ手法。
目視ではない）。

**⚠ 電気的特性表の「1.6 Ω typ」はこのボードの±15V電源の値ではない。** 表の条件は
`VDD=+13.5V, VSS=−13.5V, VS=±10V`（TMUX7612で踏んだのと同じ「表の電源電圧を読み違える」罠）。
Figure 10（実電源±15V固定）から読むと:

| 温度 | Ron@VS≈0 | flatness(\|VS\|≤10V) | Ron@\|VS\|≈13V |
|---|---:|---:|---:|
| −40°C | 6.32 Ω | 0.874 Ω | 8.88 Ω |
| **+25°C(typ)** | **7.79 Ω** | **1.063 Ω** | **10.55 Ω** |
| +85°C | 9.74 Ω | 1.251 Ω | 13.08 Ω |
| +125°C | 10.70 Ω | 1.477 Ω | 14.46 Ω |

+25°C カーブを `data/adg1407_ron_curve.json` として保存し、`switch_thd.py` で通しの THD を実測
（9.2Vrms、curve-based）:

| 部品 | 入力段のみ(Rbias47k) | 出力段のみ | 入出力通し |
|---|---:|---:|---:|
| **ADG1407** | −114.3 dB | −96.1 dB | **−95.1 dB**（Rbias を上げても頭打ち＝出力側律速） |
| DG412（参考） | — | −92.9 dB | −90.0 dB |
| TMUX7612（現行） | −166.2 dB | −115.2 dB | −115.2 dB |

**ADG1407 は TMUX7612 に対し約20dB劣るが、PT2314自身の歪み（0.1%≈−60dB）は余裕で下回る。**
DG412 が却下された理由（Rbias を上げても改善しない＝出力側律速）と同じ症状が出ており、
DG412 よりは5dB良いが同じ弱点を共有する。IC 2個で済むメリットとのトレードオフ。

**この抽出の未解決点:** 同じ Figure 7（±10/12/13.5/15/16.5V の5本重ね書き）から±13.5V条件の
カーブを分離して flatness を再計算すると 2.46 Ω となり、表の「1.6 Ω typ」と約1.5倍の開きがある
（5本の重なりで色の対応付けが厚みのある推定になった可能性。x軸の実データ範囲＝供給電圧に応じた
プロット幅からの推定で色を同定したが、確信度はFigure 10単独の抽出より低い）。**Figure 10 自体
（1本のグラフに温度4本のみ、混雑が少なく色分離の曖昧さが小さい）は単独では内的整合性が高い**
（4本が物理的に妥当な単調順序＝高温ほど高Ron、を示す）ので、上表はFigure 10由来の数値として
相応の信頼度で扱ってよいが、他部品より一段低い confidence として扱うこと。

## `switch_offiso.py --broadcast` — 入力ブロードキャスト構成でのBUS合成誤差（2026-09-02）

`solve()`（既存）は「入力OFF→DUT→出力OFF」の2段OFFモデルで、旧アーキテクチャ
（入出力とも切替）向け。**入力を全DUTへ常時配信し出力だけ切替える**構成では前提が違う
——非選択9chも選択chと**同じ強さの信号**を出しているので、「弱い幽霊信号の漏れ」ではなく
「ほぼ同一信号を出す9台がCoff経由でBUSに相乗りする合成誤差」になる。`solve_broadcast()`
を追加し、非選択9chに意図的なゲイン/位相ミスマッチとDUT自身のTHDを持たせる感度分析も
できるようにした（`--broadcast --gain-pct --phase-deg --dut-thd`）。

**結果: 確認した4部品（TMUX7612/TMUX4821/ADG1407/DG507B）すべてで、BUS合成誤差は無視できる。**

| | 誤差なし・20kHz | ±1%gain/±1°phase(最悪ケース)・20kHz | DUT自身THD(-94dB)漏れ・20kHz<br>RSS / coherent |
|---|---:|---:|---:|
| TMUX7612 | 0.0000 dB | −0.0000 dB | −203.7 / **−194.2** dB |
| TMUX4821 | 0.0000 dB | −0.0008 dB | −148.9 / **−139.4** dB |
| ADG1407 | 0.0000 dB | −0.0000 dB | −190.6 / **−181.1** dB |
| DG507B | 0.0000 dB | −0.0000 dB | −189.7 / **−186.2** dB |
| MAX14753 | 0.0000 dB | −0.0002 dB | −156.8 / **−147.3** dB |
| MAX14778 | 0.0000 dB | −0.0000 dB | −178.8 / **−169.3** dB |

**漏れ歪みは RSS（位相ランダム）と coherent（9ch 同相＝最悪）の両方を出す**
（`--broadcast --dut-thd`）。全 DUT は同一正弦波を受けているので同じ次数の高調波が
同相で揃う可能性があり、そのとき 9ch では RSS の √9 倍 = **+9.5dB** 悪化する。
最悪の TMUX4821 でも −139.4dB。**ただしこの1点だけで「非問題」と断定してはいけない** ——
下の「20kHz 単発で判断しない」を読むこと。

### OFF漏れを「20kHz 単発」で判断しない（#33 で再指摘、2026-09-03）

`TMUX4821` の −139.4 dB は `OPA1612`（−136 dB）と **3.4 dB しか離れていない**。
振幅比 0.68 なので同相で重なれば **合成 −131.5 dB** になり、DUT の −136 dB を数dB動かせる。

ただしこの数字は条件が混ざっている。**OFF 結合は容量性主体なので漏れは周波数に比例し、
非選択ソケットに何が刺さっているかで絶対値が変わる。**

    python3 AudioV2/spice/switch_offiso.py --harmonics

基本波 1kHz の THD 試験で実際に見るのは H2=2k / H3=3k / H5=5k:

| 非選択9ch | 部品 | H2=2k | H3=3k | H5=5k | 20kHz |
|---|---|---:|---:|---:|---:|
| NE5532(−94dB) | TMUX7612 | −214.2 | −210.7 | −206.2 | −194.2 |
| NE5532(−94dB) | **TMUX4821** | **−159.4** | **−155.9** | **−151.4** | **−139.4** |
| OPA1612(−136dB) | TMUX4821 | −201.4 | −197.9 | −193.4 | −181.4 |

**10石を比較する装置なので「最良の石を測っている最中に、残り9ソケットに最悪の石が
刺さっている」は現実に起きる**（表の1〜2行目がその条件）。それでも:

- **基本波 1kHz なら TMUX4821 でも H2 で −159.4 dB**、`OPA1612` の −136 dB より 23dB 下。余裕はある
- −139.4 dB が効くのは **基本波を 10kHz まで掃引して H2 が 20kHz に来たとき**だけ。
  その領域ではオペアンプ自身の THD も上がるので、−136 dB を保つ前提の方が先に崩れる
- `TMUX7612` はどの周波数でも 55dB 以上の余裕があり、この論点の影響を受けない

**用途別の結論:** PT2314 通常経路とリスニング比較では完全に無視してよい。
DIRECT 絶対 THD でも 1kHz 基本波なら余裕があるが、**高域まで掃引するなら `TMUX4821` は
再確認が要る**。なお ②の ADC 側（THD+N −102 dB）が先に効くので、実務上はそちらの実測が先。

理由は単純: `Coff` がどの部品も sub-pF〜約100pF しかなく、20kHz でもインピーダンスが
（選択ch側の `Ron+47Ω` に対して）桁違いに高い。非選択chが選択chと**同じ信号**を出している
分、漏れても「相関のある信号がわずかに位相/振幅ズレて足される」だけで、独立ノイズのように
は効かない。**この構成では OFF isolation は、ON経路の THD（`switch_thd.py` 側）ほどには
効かない**——ただし `TMUX4821` の高域だけは上記のとおり例外扱いにする。TMUX4821 だけ ±1%/±1° 感度で −0.0008dB と非ゼロだが、
これも実用上無視できる水準。

## `switch_compare.py` — 候補5部品の同条件比較（2026-09-02）

入力ブロードキャスト＋出力のみMUX構成での最終比較。条件は
**±15V電源 / ゲイン2 / 出力最大 9.2Vrms(±13Vpk) / 負荷50kΩ / Riso 47Ω / 1kHz**。

    python3 AudioV2/spice/switch_compare.py

| 部品 | IC数 | 確度 | 4.0Vrms | 7.07Vrms | 8.0Vrms | **9.2Vrms** |
|---|---:|---|---:|---:|---:|---:|
| TMUX7612 (4×SPST) | 5 | 実カーブ | −166.2 | −166.8 | −136.1 | **−115.2 dB** |
| ADG1407 (dual 8:1) | 2 | 実カーブ | −117.8 | −105.3 | −101.6 | **−96.1 dB** |
| DG507B (dual 8:1) | 2 | 実カーブ | −84.6 | −75.6 | −72.5 | **−70.3 dB** |
| MAX14753 (dual 4:1) | 3 | モデル | −158.1 | −148.5 | −146.4 | **−144.0 dB** |
| MAX14778 (dual 4:1) | 3 | モデル | −168.7 | −167.3 | −166.5 | **−165.3 dB** |

`|V|≤13V` での Ron(V) の広がり（これが歪みを決める。絶対Ronではない）:

| 部品 | Ron@0V | flatness |
|---|---:|---:|
| TMUX7612 | 1.11 Ω | 0.313 Ω |
| ADG1407 | 7.82 Ω | 2.758 Ω |
| DG507B | 139.8 Ω | 62.151 Ω |
| MAX14753 | 60.0 Ω | 0.013 Ω |
| MAX14778 | 0.84 Ω | 0.001 Ω |

**絶対 Ron は歪みをほとんど決めない。** MAX14753 は Ron 60Ω で TMUX7612 の44倍あるが、
**仮定モデル上では** 9.2Vrms で29dB良い（実 Ron(V) 由来の値ではない。後述の確度を参照）。
50kΩ負荷では Ron 130Ω（max）でも挿入損失は −0.031dB に過ぎない。
効くのは信号振幅にわたる Ron の変動幅だけ。

### 判定の基準線 — 3つの「床」を混ぜないこと

> **ここは3回間違えた場所**なので経緯ごと残す（すべて [issue #33](https://github.com/indare/pcb_work/issues/33)）。
> 1. 「測定床 = PCM1804 の DR 112dB」→ **DR は広帯域の雑音指標で、歪みの床ではない**
> 2. 「FFT 処理利得 30.1dB を足して −142.1dBFS」→ **処理利得が効くのはランダム雑音だけで、
>    ADC 自身の高調波（離散トーン）には効かない**。加えて実使用 N は 1024、窓は Hann
> 3. 「THD+N から SNR を引いて ADC の高調波は −102.6dB」→ **THD+N は無加重、SNR は A特性で
>    条件が揃っておらず引き算できない**。さらに「床より下だから見えない」も誤り（合成は残る）
>
> 共通する誤りは **「雑音の床」と「歪みの床」を同じものとして扱ったこと**、そして
> **「下にあれば無視できる」と考えたこと**。同じ次数の成分は複素ベクトル和で必ず残る。

**性質の違う量が3つあり、用途ごとに効くものが違う。そして「下にあれば無視できる」わけではない。**

> **⚠ ①②はどちらも「確定した測定床」ではない**（#33、2026-09-03）。
> **理想化した概算**であって、実測で置き換えるべき目安。**これを根拠に部品を落とさないこと。**

| # | 量 | 概算値 | FFT長で下がるか | 性質 |
|---|---|---:|---|---|
| ① | 理想化した bin noise floor | 約 **−137 dBFS** | **下がる** | **雑音**。白色仮定を置いた概算 |
| ② | PCM1804 の **THD+N 基準値** | typ **−102 dB** | **下がらない** | **歪み+雑音の総量**。次数別内訳は不明 |
| ③ | 前段の歪み（PT2314） | −60 dB | — | 通常経路では常にこれが支配 |

- **絶対 THD で効くのは ②側**（①は雑音なので歪みの床ではない）
- ただし ②は **THD+N のデータシート基準値**であって、**「絶対 THD の床」そのものではない**。
  **実際の高調波測定限界は、これから行う H2/H3 baseline 実測で決まる**

#### ① 理想化した bin noise floor（約 −137 dBFS）← **概算。実測で置き換える**

`DR = 112 dB`（**A特性**、`fS=48k`）に FFT の処理利得を足した**理想化値**。2点直した:

- **実使用 N は 1024**。`spectrum.py` の `MAX_N = 2048` はコメントどおり
  「起動時に確保する最大点数」で、`SpectrumAnalyzer.__init__` の既定は `n=1024`、
  `spectrum_lcd.py` も `FFT_SIZES = (1024, 2048)` / `self.ni = 0` なので既定は 1024。
  2k は明示的に切り替えたときだけ
- **窓は Hann**（`fft.py:51` `_hann`）なので ENBW ≈ 1.5 bin ぶん（1.76 dB）床が上がる

    N=1024: 10log10(512) − 10log10(1.5) = 27.09 − 1.76 = 25.33 dB → 約 −137 dBFS
    N=2048: 30.10 − 1.76 = 28.34 dB → 約 −140 dBFS

> **⚠ これは確定値ではない。** `112 dB` は **A特性の積分値**なので、
> **雑音スペクトルが白色・平坦だと仮定しない限り、ここから正確な 1 bin の floor は決まらない**。
> ΔΣ 変調器は原理的に雑音整形をしていて高域が持ち上がるので、平坦ではない。
> **実際の bin noise floor は ADC 無入力の実測で確定させること。**

#### ② PCM1804 の THD+N 基準値（typ −102 dB）← **床そのものではない**

| 条件 | THD+N |
|---|---:|
| `VIN = −0.5 dB`, `fS=48k/256fS` | **−102 dB** |
| `VIN = −0.5 dB`, `fS=96k/192k` | −101 dB |
| `VIN = −60 dB` | −47〜−49 dB（信号比。絶対では約 −107 dBFS） |

> **⚠ 「THD+N から SNR を引いて高調波分 −102.6 dB」という計算は取り下げた**（#33、2026-09-03）。
> **2つの値は測定条件が揃っていない。** データシート p.7 の脚注より:
> - **THD+N**: `fIN = 1 kHz`, Audio Precision System Two, RMS mode, **20-kHz LPF + 400-Hz HPF**（無加重）
> - **SNR / DR**: **A-weighted** と明記
>
> 加重の違う量を線形電力で引き算することはできない。**ADC 自身の H2/H3 の実値は不明。**

したがって言えるのはここまで:

- `THD+N typ ≈ −102 dB` は **TI 公式値**（この数字自体は正しい）
- **絶対 THD の測定能力はこの近辺で制約される可能性が高い**
- ただしこれは **THD+N（歪み+雑音の総量）の基準値**であって、**「絶対 THD の床」ではない**
- **実際の高調波測定限界は、ADC 直結の H2/H3 baseline 実測で決まる**

#### 「床より下だから見えない」は言い過ぎ（合成を忘れない）

ADC の高調波と切替素子由来の高調波は、**同じ次数なら複素ベクトル和**になる。
差が何dBあってもゼロにはならない。同相最悪の誤差は `20log10(1 + 10^(−margin/20))`:

| 下位成分とのマージン | 同相最悪の誤差 |
|---:|---:|
| 13 dB | +1.75 dB |
| 18 dB | +1.03 dB |
| 25 dB | +0.48 dB |
| 30 dB | +0.27 dB |
| 39 dB | +0.10 dB |

> **⚠ この式を PCM1804 と切替素子にそのまま当てはめてはいけない**（#33、2026-09-03）。
> **合成できるのは同じ次数の成分どうし**だが、比較しようとしている2つはどちらも「総量」:
> - `−102 dB` は **THD+N の総量**（次数別の内訳は不明）
> - 切替素子の `−115.2 dB` は **H2〜H9 を RSS した総 THD**
>
> **総量どうしをベクトル合成しても意味がない。** 上の表は
> **「仮に同一次数の成分がそのレベルにあれば」という一般式の例示**として読むこと。
> `PCM1804` と `TMUX7612` が実際に何dB動くかは、**ADC の実測 H2/H3 と素子の H2/H3 を
> 次数別に突き合わせるまで分からない。**

#### 切替素子側は次数別に出せる（ADC 側が未知なだけ）

`switch_thd.py` は H2 / H3 を総 THD と別に返しているので、素子側は次数別に見られる。

    python3 AudioV2/spice/switch_compare.py

> **⚠ 2026-09-03 に条件バグを修正**（#33）。この表を最初に出したとき
> `stage="both"` / `rbias=100k` をハードコードしており、**上の総 THD 比較表
> （`--stage` 既定 `output`、`--rbias` 既定 47k）と違う回路を計算していた**。
> 現行構成は入力ブロードキャスト＋**出力のみ MUX** なので `output` が正しい。
> 修正後は **THD(RSS) 列が上の表と完全一致**する（一致していなければ条件がずれている）。

| 部品 | Vrms | THD(RSS) | H2 | H3 |
|---|---:|---:|---:|---:|
| TMUX7612 | 4.0 | −166.2 | −166.6 | −181.1 |
| **TMUX7612** | **9.2** | **−115.2** | **−121.2** | **−119.7** |
| ADG1407 | 4.0 | −117.8 | −133.6 | −117.9 |
| **ADG1407** | **9.2** | **−96.1** | **−133.4** | **−96.3** |
| DG507B | 4.0 | −84.6 | −91.2 | −85.9 |
| DG507B | 9.2 | −70.3 | −73.1 | −77.4 |

**総 THD で比べると像が変わる部品がある:**

- **`ADG1407` は H3 支配。** 9.2Vrms で **H2 −133.4 / H3 −96.3 と 37 dB も違う**。
  総 THD（−96.1）だけ見ていると、この偏りが完全に隠れる。
  H2 は `TMUX7612` @9.2Vrms（−121.2）より 12 dB も**良い**
- `TMUX7612` @9.2Vrms は総 THD −115.2 だが、**個々の次数は −121.2 / −119.7 で
  総量より 4〜6 dB 低い**。「ADC の −102 dB と 13 dB 差」という比較は
  総量どうしの比較で、次数別ではない
- `DG507B` は逆に H2 支配（9.2Vrms で H2 −73.1 / H3 −77.4）
- `MAX14753`/`MAX14778` の H2 が両振幅で同値（−169.1 dB）なのは**数値床**。
  モデルが左右対称パラボラなので偶数次が原理的に出ない。**実チップの値ではない**

#### 経路歪みリスクの一次スクリーニング（ADC 非依存）

> **⚠ 「②より上だから確実に測定を汚す」は撤回した**（#33）。①②を確定床ではないと書いた
> 直後に②を確定床として部品を落としており、自己矛盾だった。
> 代わりに **ADC を経由しない基準**を使う —— スイッチ自身の H2/H3 を DUT の THD と比べる。
>
> **ただしこれも「判定」ではなく「スクリーニング」。条件が揃っていない**（#33、2度目の指摘）:
> - **switch 側** = この計算の実条件（`G=2` / `Vout 4.0・9.2 Vrms` / `RL 50k` / ±15V）
> - **DUT 側** = **データシートの参考値**（一般に `G=1` / `3 Vrms` / `RL 2k・600Ω`）
> - さらに switch は **H2/H3 の最大次数**、DUT は **THD+N の総量**（内訳不明）
>
> **保守的なふるい分けには使えるが、「その石は測れない」の証明にはならない。**
> 厳密な判定には**同一条件（同 gain / Vout / load）の DUT 特性か実測**が要る。
> なお DUT のデータシートはこのリポジトリに未取得で、上記条件は未検証。

    python3 AudioV2/spice/switch_compare.py

| 部品 | Vrms | 最大次数 | NE5532 −94 | MUSES02 −100 | LME49860 −130.5 | OPA1612 −136 |
|---|---:|---:|---:|---:|---:|---:|
| TMUX7612 | 4.0 | −166.6 | −72.6 | −66.6 | −36.1 | −30.6 |
| TMUX7612 | 9.2 | −119.7 | −25.7 | −19.7 | **要注意** | **要注意** |
| ADG1407 | 4.0 | −117.9 | −23.9 | −17.9 | **要注意** | **要注意** |
| **ADG1407** | **9.2** | **−96.3** | −2.3 | **要注意** | **要注意** | **要注意** |
| **DG507B** | **4.0** | **−85.9** | **要注意** | **要注意** | **要注意** | **要注意** |
| **DG507B** | **9.2** | **−73.1** | **要注意** | **要注意** | **要注意** | **要注意** |

「要注意」＝ switch の高調波成分が DUT の公称総量を上回る。**「測れない」ではない。**

- **`DG507B` は 4Vrms でも全 DUT で要注意。差が大きく条件差では覆らないので不適**
  （ADC を持ち出すまでもない）
- **`ADG1407` @9.2Vrms は H3≈−96dB。** 低歪比較系には高リスク。
  落とすなら「②より上だから」ではなく**「素子自身が H3 −96dB を出すから」**
- **⚠ `TMUX7612` も 9.2Vrms では H3 が −119.7 dB まで悪化する**（4Vrms は −166.6 dB）。
  **「9.2V では `OPA1612` を絶対測れない」とまでは言えない**（条件不一致）が、
  **46 dB の悪化は低歪比較系にとって余計な変数**。
  **「膝（±11V）より下で使う」の設計理由としては、これで十分強い**

厳密な可否は**同一条件の DUT 特性 or ADC baseline 実測**後に確定する。

#### ⚠ 繰り返している失敗パターン: 「条件を揃えずに数値を比べる」

**この議論だけで3回やっている。**（すべて [issue #33](https://github.com/indare/pcb_work/issues/33) で指摘された）

| # | やったこと | なぜ駄目か |
|---|---|---|
| 1 | `THD+N`(無加重) − `SNR`(A特性) | **加重が違う量**は引き算できない |
| 2 | 総THD表は `a.stage`、次数別表は `"both"` 固定 | **同じスクリプト内で違う回路**を計算 |
| 3 | switch(G2/9.2Vrms/50k) vs DUT(DS の G1/3Vrms/2k) | **測定条件が違う量**を直接比較 |

**数値を並べる前に、必ず「この2つは同じ条件で測られたものか」を確認すること。**
データシートから数字を拾うときは、**値だけでなく脚注の条件も一緒に写す。**
（1 は同じデータシートの同じページに条件が書いてあったのに読まなかった）

#### 「相対比較は成立する」も条件付き

「ADC の歪みは全 DUT に共通に乗るからキャンセルする」は**単純には成り立たない**。
観測される H2 は `H2_ADC + H2_DUT` の**複素ベクトル和**で、DUT ごとに振幅も位相も違うので、
ADC の −102 dB に対してわずかに足したり引いたりする（−136 dB の DUT なら最大 ±0.17 dB）。

| 用途 | 可否 |
|---|---|
| 大きな THD 差の相対比較 | **十分可能** |
| −90〜−100 dB 級 と −130 dB 級 の区別 | 条件次第 |
| −130 dB vs −136 dB の絶対比較 | **そのままではかなり難しい** |
| baseline を**複素振幅**で測って差分を取る | 可能性あり |

**正確な言い方は「相対比較は成立」ではなく
「ADC の再現性と baseline 除去精度の範囲で相対比較可能」。**

#### ⚠ ②は実測で確定させること ← **ここが今の最大の未決事項**

`−102 dB` は **THD+N の typ 値**で、条件は `VIN = −0.5 dB` の1点。雑音込みなので
**ADC 自身の H2/H3 の実値ではない**し、実機では入力バッファ・基準電圧・レイアウトでも変わる。

**必要なのは SPICE ではなく実測。** 手順:

1. **ADC 直結 baseline** — 基準信号を直接 ADC へ入れ、H2/H3 の**振幅と位相**を測る
2. **同レベルで繰り返し測定** — 再現性（振幅・位相のばらつき）を見る
3. その再現性が、**「どこまでの THD 差を比較できるか」の実用的な限界**になる
4. できればレベルスイープも取る（②は `VIN` 依存。`VIN=−60dB` では絶対値が約 −107 dBFS）

**この実測が済むまで、②を根拠に部品を落とさないこと。**

### データ確度の違い（重要）

- **実カーブ**（TMUX7612 / DG412 / ADG1407 / DG507B）: データシートの Ron vs 信号電圧
  グラフをベクタパスから数値抽出。
- **モデル**（MAX14753 / MAX14778）: flatness スペックを満たす対称パラボラの仮定形状。
  MAX14753 はグラフのy軸分解能（0.0816Ω/px）が 0.03Ω の flatness を解像できず、
  MAX14778 は Ron(V) グラフ自体が存在しない。**実チップにキンクや非対称があれば外れる。**

### 手法の妥当性検証（DG507B で取れた）

DG507B のデータシートは THD **0.04% @ RL=10kΩ, 5Vrms** を公称している。
抽出カーブによる計算は 4.0Vrms/50kΩ で −84.6dB。負荷を 10k→50k にすると +14dB
効くので、公称値 −68dB は 50kΩ換算で約 −82dB に相当し、**振幅差を考えれば一致する**。
メーカー実測値と抽出＋シミュレーションが合った初めての例で、手法の裏付けになる。

## `max14753` / `max14778` の Ron(V) について（2026-09-02）

**MAX14753**: p.10「ON-RESISTANCE vs. VOUT (DUAL SUPPLIES)」はモノクロで線種違い。
色分離できないので、各水平ダッシュ群の x 範囲（=その電源条件でプロットされている
信号範囲）で同定した。

| 電源 | Ron | プロット範囲 |
|---|---:|---|
| ±35V | 55.00 Ω | −35.0 .. +34.7 V |
| ±30V | 54.91 Ω | −30.0 .. +29.7 V |
| ±20V | 54.60 Ω | −20.1 .. +19.8 V |
| ±10V | 54.40 Ω | −10.1 .. +9.8 V |

**4本とも完全にフラットで、±10V電源でもレールぎりぎりまでフラット。**
これが TMUX7612（±15V電源で±11Vから膝）との決定的な差。
なお表の flatness 0.03Ω の測定条件は **±35V電源/±20V信号**（レールまで15Vの余裕）で、
うちの ±15V/±13V ではない。グラフが「±10V電源でもレールまでフラット」を示しているので
外挿してよい、という理屈で使っている。

**MAX14778**: Ron(V) グラフが存在しない。THD+N の公称 0.003% も
**振幅条件がテーブルにもグラフにも書かれていない**（`RS=RL=1kΩ, f=20Hz to 20kHz` のみ）
ため 9.2Vrms 条件との直接比較には使えない。flatness 3mΩ（−25V≤VIN≤+25V 全域、
ICOM=−300mA）だけが手がかり。

## MAX14778 の charge-pump スプリアスと ADC エイリアス（2026-09-02）

MAX14778 は内部に3つのチャージポンプを持つ。p.9「CHARGE PUMP NOISE — SWITCH ENABLED」
（MEASURED AT BCOM, RBW=5.1kHz）をベクタ抽出したピーク:

| 周波数 | レベル |
|---:|---:|
| 35 MHz | **−74.6 dB** |
| 71 MHz | **−76.4 dB** |
| （フロア） | −101 dB |

AudioV2 の計測系は `PCM1804`、**48kHz / 256fs（SCKI=12.288MHz）マスタ**。
前段のアンチエイリアスは `R709-712 (1kΩ) + C713-716 (1.8nF C0G)` の**1次RC、fc≈88kHz**。
1次なので 35MHz で約 52dB、71MHz で約 58dB の減衰。
スプリアス −75dB を仮に dBV とすると 178µV、RC 後で約 0.45µV。
PCM1804 は FS ±2.5V・DR 約112dB なので自身の雑音フロアが約 8.9µV あり、
**最悪（帯域内へ丸ごと折り返した場合）でも ADC 雑音フロアより約26dB下**。

エイリアス先も、12.288MHz でも 64fs=3.072MHz でも 35/71MHz は整数倍から外れている。

**結論: 一次見積では問題なさそう。ただし実測必須。** 以下3点が未確定なので、
この計算を「ブロッカーではない」の根拠にしてはいけない。

1. **グラフの dB 基準が明示されていない。** 上の 178µV は dBV 仮定。dBm/50Ω なら別値
2. **35/71MHz が信号ピンからだけ来るとは限らない。** 内部チャージポンプなので
   電源・GND 経由の結合もありうる。その経路は入力側の 1次RC では落ちない
3. **エイリアスは「クロックの整数倍から外れている」だけでは保証できない。**
   ΔΣ変調器の実効サンプリングと折り返しは単純な整数倍判定では決まらず、
   スプリアスの実周波数も ±数MHz の幅がある

## `input_topology.py` — 入力側 A / B / C 案（2026-09-02）

入力ブロードキャスト化にあたり、バイアスの持たせ方3案を比較した。

    A案: PT2314 ─┬─[C]─┬─Rb(100k)─GND        （×10）
    B案: PT2314 ─[C]─┬─Rb_c(10k)─GND ├─[Rs]─ DUT （×10）
    C案: PT2314 ─[共通バッファ]─┬─[C]─┬─Rb(10〜22k)─GND （×10）

PT2314 の特性規定は RL=10kΩ なので、直結する A/B 案は「10本並列で 10kΩ 以上」の
制約を受ける（A案 Rb=100k、B案 Rb_c=10k）。C案はバッファが受けるので Rb を小さくできる。

**交流と直流で効き方が違うのが要点。** 結合Cが直流を切るので、DC 経路は Rb だけ
＝オフセットは Rb に比例する。一方 AC では結合Cが短絡なので、雑音は前段の出力抵抗
（PT2314 typ **1.9Ω**）が支配し、Rb にほとんど依存しない。

| | A案 Rb=100k | B案 Rb_c=10k | C案 Rb=22k | C案 Rb=10k |
|---|---:|---:|---:|---:|
| 前段が見る負荷 | 10.0 k | 10.0 k | 2.2 k | 1.0 k |
| 必要な結合C（-3dB 2Hz） | **0.80 µF** | 7.96 µF | 3.62 µF | 7.96 µF |
| フィルム化 | **現実的** | 非現実的 | やや大きい | 非現実的 |
| 雑音 @1kHz [nV/√Hz] | 0.195 | 1.295 | 0.056 | 0.048 |
| 雑音 @20Hz | 4.040 | 1.818 | 1.894 | 1.277 |
| Ib×Rb（LT1364 typ/max） | 60 / **200** mV | 6 / 20 mV | 13 / 44 mV | 6 / 20 mV |
| Ib×Rb（NE5532 typ/max） | 20 / 80 mV | 2 / 8 mV | 4.4 / 17.6 mV | 2 / 8 mV |
| 結合C漏れ×Rb（電解上限） | 300 mV | 30 mV | 66 mV | 30 mV |
| 結合C漏れ×Rb（**フィルム**） | **0.036 mV** | — | — | — |
| 部品 | C×10+R×10 = 20 | C×1+R×1+Rs×10 = 12 | buf+C×10+R×10 | 同左 |
| ch 独立性 | 独立 | **全ch連動** | 独立 | 独立 |

### 電解コンデンサの漏れ電流について

PT2314 の出力は直流 4.5V（データシート `DC Voltage Level` typ）なので結合Cには
直流がかかる。アルミ電解の漏れ電流スペックは一般に `0.01·C·V または 3µA の大きい方`
で、**A案の 100k と組むと最大 300mV** になる。これは Ib×Rb より大きい。

**ただし A案は Rb が大きいぶん必要な結合容量が 0.80µF まで下がり、フィルムが使える。**
フィルムの絶縁抵抗（10,000MΩ·µF 級）なら漏れは 0.036mV で消える。
逆に C案は Rb が小さいので必要容量が 3.6〜8µF になり、フィルムは非現実的＝電解確定。
**「電解の漏れ」は A案の弱点ではなく、A案でだけ回避できる。**

### 20Hz で A案の雑音が上がる理由

0.80µF は 20Hz で 9.9kΩ あり、Rb=100k との並列の実部が 982Ω になるため
4.04nV/√Hz まで上がる（1kHz 以上では 0.2nV/√Hz 未満）。オペアンプ自身の
雑音（2〜8nV/√Hz）と同程度なので実用上は問題ないが、C案より不利な唯一の帯域。

### 判断

- **A案 + 1µF フィルム結合C** — 1kHz 以上の雑音・漏れ電流とも最小、ch 独立、
  前段の追加なし。残る代償は Ib×100k の直流オフセット（LT1364 max 200mV）だが、
  出力は 47Ω+2.2µF で AC 結合されるので後段には出ず、ヘッドルームにも影響しない
- **B案** — 共通ノードの直流が「刺さっている全10石の Ib の和」で決まり、
  1石差し替えると全chが動く。比較装置としては筋が悪い
- **C案** — Ib の影響を 1/5〜1/10 にでき、現行の Rb=1k に近い条件で比較できる点は
  A案より優れる。代償は (a) バッファ自身の歪み・雑音が全DUTの前に入る
  (b) 電解C確定 (c) 部品と電源が増える。
  **(a) は全chに共通なので「比較」には効かないが、DIRECT 経路で「オペアンプ単体の
  絶対THD」を測る用途では新しい床になる**（在庫最良の OPA1612 が −136dB なので、
  バッファもそれ以上でなければ意味がない）。その用途を残すならDIRECTはバッファも迂回が要る

**LT1364 級の 200mV を許容できるなら A案 + フィルム。許容できないなら C案。**
この判断は「Ib×Rb の直流オフセットを、比較条件の乱れとしてどこまで嫌うか」に尽きる。

## 前提と限界

- オペアンプは理想（電圧源）で置いている。GBW・スルーレート・出力インピーダンスは見ていない
- スイッチは ngspice の `SW` モデルに `Ron` と漏れ電流を与えただけ。
  チャージインジェクションと寄生容量は入っていない
- **切替時のクリック音そのもの**（チャージインジェクション由来）は別問題で、
  ここでは扱っていない。TMUX7612 の 2 pC は ADG1412 の 1/10
