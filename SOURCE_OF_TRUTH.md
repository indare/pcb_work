# source of truth — 何をどこに書くか

このリポジトリには「同じことが2箇所に書いてある」ことによる事故が繰り返し起きている。
その予防のための方針。**エージェント（Claude / Cursor）も人間もこれに従う。**

関連: シートごとの「生成スクリプト vs KiCad」の所有権は
[AudioV2/AGENT_HANDOFF.md §2.8](AudioV2/AGENT_HANDOFF.md) が正。ここでは重複させない。

---

## 1. 判断ロジック（これ1問）

> **その情報は回路図から機械的に導出できるか？**

| | 正 | 具体例 |
|---|---|---|
| **導出できる** | **KiCad の回路図** | ネットリスト、参照(designator)、ピン接続、部品数、部品値 |
| **導出できない** | **ドキュメント** | 決定と理由、却下した代替案、外部世界（パネル配置・現物端子・箱配線）、意図・制約・未確定事項 |

導出できる情報をドキュメントに書き写した時点で、それは**必ず腐る負債**になる。書くなら生成する。

---

## 2. なぜこの方針なのか（2026-09-01 の実例）

KiCad で再アノテーションした結果、58件の参照が振り直された。そのとき壊れたもの:

- `PARTS.md` の「R705–708 = 20 kΩ」→ 振り直しで連番でなくなり**嘘になった**
- `AGENT_HANDOFF.md` の「I2Cプルアップは `R210`/`R211`」→ 実物は `R501`/`R502`
- `wire_circuit_design.py` が旧参照をハードコードしており、**再生成するとアノテーションが巻き戻る**状態だった

いずれも「回路図から導出できる事実を手書きしていた」ことが原因。

---

## 3. 語彙のルール — designator でなくネット名で書く

同じアノテーションで、**動いたもの／動かなかったもの**がはっきり分かれた。

| | 例 | 2026-09-01 の再アノテーションで |
|---|---|---|
| **designator** | `U301`, `R210`, `C205`, `SW1`, `RV101` | **58件が変わった** |
| **ネット名 / 機能名** | `CH1_SET`, `I2C_SDA`, `TONE_L`, `AMP_SEL_L` | **1件も変わらなかった** |

designator は **KiCad が採番する割り当ての産物**で、再アノテーションで動く。
ネット名は**設計者が決めた意図**なので動かない。だからドキュメントはネット名で書く。

```
❌ 「I2Cプルアップは R210/R211 の 4.7 kΩ」
⭕️ 「I2Cプルアップは ControlPanel 側にのみ置く（並列化を避けるため RelayBoard には置かない）」
```

後者は designator を含まないのに情報量が落ちておらず、しかも**なぜそうするか**という
導出不能な情報（＝ドキュメントが本来担うべきもの）まで書けている。

---

## 4. それでも designator が要る場所

BOM・発注・パネルの現物対応表は designator が本質的に必要。
ここは**手書きせず回路図から生成する**。

```bash
docker/kicad-cloud-build/kicad-run.sh cli sch export bom -o @OUT@/bom.csv @WORK@/AudioV2/AudioV2Case.kicad_sch
```

手書きが残っている箇所（`PARTS.md` の Amp 値表など）は、生成に寄せるまでの暫定と考える。

---

## 5. 実務チェックリスト

ドキュメントに1行足すとき:

1. これは回路図を見れば分かることか？ → **Yes なら書かない**
2. 書く必要があるなら、designator でなくネット名・機能名で書けないか？
3. designator を書くしかないなら、それは BOM 系か？ → **生成に寄せる**
4. どれでもない（＝決定理由・外部世界・意図）→ **ドキュメントが正。しっかり書く**

回路図を変えたとき:

- 参照が変わる操作（再アノテーション等）をしたら、`wire_circuit_design.py` と
  `*.md` の追随が必要か確認する。**追随が必要になった時点で、それは 1. 違反のサイン**
- 検証は `docker/kicad-cloud-build/kicad-run.sh erc` / `netlist`
